server:
  port: 8001
spring:
  application:
    name: consul-producer
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: consul-producer
        register-health-check: false
